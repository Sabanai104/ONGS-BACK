version: "3.9"

services:
  api:
    container_name: myongs_api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    # env_file:
    #   - .env
    command: npm run dev